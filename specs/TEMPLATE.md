# NNNN Short name

**Status:** draft | implemented
**Date:** YYYY-MM-DD

## Goal

One or two sentences on what this is for, in terms a game author would care about.

## Behavior

What the engine does, stated plainly. Numbers, units, and edge cases belong here.
Say what happens when things are missing or wrong, not only the happy path.

## Acceptance criteria

Each line is a claim that can fail, followed by the test that proves it.

- The thing does X when Y. — `module::tests::test_name`
- The thing does Z when nothing is available. — `module::tests::other_test_name`

### Verified by hand

Criteria that need a GPU, a window, or a speaker. Say how to check each one.

- Something visible looks right. — run pong and watch for it.

## Out of scope

What this deliberately doesn't do, so the next reader doesn't file it as a gap.
